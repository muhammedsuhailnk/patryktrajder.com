export default class LanguageDropdown {
    private readonly dropArrow;
    private readonly list;
    private isExpanded;
    constructor(dropdown: HTMLElement);
    static getLanguage(): string;
    private changeLanguage;
    private toggle;
}
