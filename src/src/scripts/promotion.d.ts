export default class Promotion {
    private readonly oldPriceValueElement?;
    private readonly oldPriceElement;
    private readonly priceElement;
    private readonly priceValueElement;
    private readonly promotionInfo;
    constructor(product: HTMLElement, endDate: Date);
    private endPromotion;
}
