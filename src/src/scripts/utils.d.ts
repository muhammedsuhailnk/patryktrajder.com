export default class Utils {
    static mod(n: number, m: number): number;
    static modNeg(n: number, m: number): number;
    static nthIndex(originalString: string, searchString: string, n: number): number;
    static remToPx(nRem: number): number;
}
